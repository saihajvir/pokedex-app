import { colors } from "./colors"

export const themes = {
    default: {
        body: "#EFE6DD"
    },

    dark: {
        body: colors.darkgrey
    }
}

export const comp_themes = {
    default: {
        switch_border_color: colors.darkgrey,
        switch_bg_color: colors.darkgrey,
    },

    dark: {
        switch_border_color:'white',
        switch_bg_color:'white',
    }
}